{{ include('header_gerenciar.twig') }}
<div class="row">
  <div class="col-md-12 col-sm-12 col-xs-12">
    <div class="x_panel">
      <div class="x_title">
        <h2>Integração
          {{ $old_integracao->getNome() }}</h2>
        <div class="clearfix"></div>
      </div>
      <div class="x_content">
        <form id="integracao-form" class="form-horizontal form-label-left" method="post" action="/gerenciar/integracao/editar?id={{ $integracao->getID() }}" enctype="multipart/form-data">
          {{ include('form_integracao.twig') }}
          <div class="form-group">
            <div class="col-md-12 col-sm-12 col-xs-12 text-center">
              <div class="ln_solid"></div>
              <button type="submit" class="btn btn-success">Salvar</button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
{{ include('footer_gerenciar.twig') }}